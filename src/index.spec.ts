import { someValue } from ".";

// This is an example test setup, this should pass.
describe("101 ways example test that always passes", () => {
  it("should work", () => {
    expect(someValue).toBe(1);
  });
});
